name: Snyk Security Scan

on:
    push:
        branches: [master]
    pull_request:
        branches: [master]

permissions:
    contents: read
    security-events: write


jobs:
    snyk_scan:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-node@v4
          with:
            node-version: '20'
            cache: 'npm'
        - name: Install all dependencies
          run: npm install --workspaces
        - name: install snyk cli
          run: npm install -g snyk
        - name: Run Snyk scan
          continue-on-error: true
          env:
            SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
          run: snyk test --all-projects --json > snyk-results.json 2>&1
        - name: Run Snyk SARIF scan
          continue-on-error: true
          env:
            SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
          run: snyk test --all-projects --sarif-file-output=snyk.sarif
        - name: Upload SCA results to GitHub
          if: always()
          uses: github/codeql-action/upload-sarif@v3
          with:
            sarif_file: snyk.sarif
            category: snyk-sca
        - name: Run Snyk Code (SAST) scan
          continue-on-error: true
          env:
            SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
          run: snyk code test --all-projects --sarif-file-output=snyk-code.sarif
        - name: Upload SAST results to GitHub
          if: always()
          uses: github/codeql-action/upload-sarif@v3
          with:
            sarif_file: snyk-code.sarif
            category: snyk-code